import React from "react"
import ReactDOM from "react-dom/client"

const title = <h1 id="h1">Hello World....</h1>


const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(title);